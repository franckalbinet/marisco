"""Several dictionaries used to generate `.toml` configuration files copied under `/home/.marisco` folder and associated utilities function. These `.toml` files can be then adapted to your specific needs if required.

Docs: https://franckalbinet.github.io/mariscoapi/configs.html.md"""

# AUTOGENERATED! DO NOT EDIT! File to edit: ../nbs/api/configs.ipynb.

# %% auto #0
__all__ = ['AVOGADRO', 'NC_DIM', 'NC_VARS', 'CSV_VARS', 'NC_GROUPS', 'SMP_TYPE_LUT', 'NC_DTYPES', 'CSV_DTYPES', 'ZOTERO_LIB_ID',
           'NETCDF_TO_PYTHON_TYPE', 'lut_path', 'lut_fname', 'nc_tpl_path', 'cache_path', 'get_time_units', 'sanitize',
           'get_lut', 'Enums']

# %% ../nbs/api/configs.ipynb #3f92bc7b
from pathlib import Path
import os
import re
from typing import Dict
import pandas as pd
import fastcore.all as fc
from netCDF4 import Dataset
from importlib.resources import files as _pkg_files

# %% ../nbs/api/configs.ipynb #b0bee1e5
AVOGADRO = 6.02214076e23

# %% ../nbs/api/configs.ipynb #7022eb63
NC_DIM = 'id'

# %% ../nbs/api/configs.ipynb #8e6e0a30
# Lookup variables names to MARIS NetCDF4 template variable names
NC_VARS = {
    'SMP_ID': 'id',
    'SMP_ID_PROVIDER': 'id_provider',
    'LON': 'lon',
    'LAT': 'lat',
    'SMP_DEPTH': 'smp_depth',
    'TOT_DEPTH': 'tot_depth',
    'TIME': 'time',
    'AREA': 'area',
    'NUCLIDE': 'nuclide',
    'VALUE': 'value',
    'UNIT': 'unit',
    'UNC': 'unc',
    'DL': 'dl',
    'DLV': 'dlv',
    'FILT': 'filt',
    'COUNT_MET': 'count_met',
    'SAMP_MET': 'samp_met',
    'PREP_MET': 'prep_met',
    #'DRY_MET': 'dry_met', # NOT IN USE, no LUT for this. 
    'VOL': 'vol',
    'SAL': 'sal',
    'TEMP': 'temp',
    'PH': 'ph',
    'BIO_GROUP': 'bio_group',
    'SPECIES': 'species',
    'BODY_PART': 'body_part',
    'SED_TYPE': 'sed_type',
    'TOP': 'top',
    'BOTTOM': 'bottom',
    'DRYWT': 'drywt',
    'WETWT': 'wetwt',
    'PERCENTWT': 'percentwt',
    'LAB': 'lab',
    'PROFILE_ID': 'profile_id', # NEED TO INCLUDE.
    'STATION': 'station'
    }

# %% ../nbs/api/configs.ipynb #b71a2b1b
CSV_VARS = {
    'LON': 'longitude', 
    'LAT': 'latitude',
    'SMP_DEPTH': 'sampdepth',
    'TOT_DEPTH': 'totdepth',
    'TIME': 'begperiod',
    'AREA': 'area',
    'NUCLIDE': 'nuclide_id',
    'VALUE': 'activity',
    'UNIT': 'unit_id',
    'UNC': 'uncertaint',
    'DL': 'detection',
    'DLV': 'detection_lim',
    'FILT': 'filtered',
    'COUNT_MET': 'counmet_id',
    #'DRY_MET': 'drymet_id', # NOT IN USE, no LUT for this. 
    'SAMP_MET': 'sampmet_id', 
    'PREP_MET': 'prepmet_id',
    'VOL': 'volume',
    'SAL': 'salinity',
    'TEMP': 'temperatur',
    'SPECIES': 'species_id',    
    'BODY_PART': 'bodypar_id',
    'SED_TYPE': 'sedtype_id',
    'TOP': 'sliceup',
    'BOTTOM': 'slicedown',
    'DRYWT': 'drywt',
    'WETWT': 'wetwt',
    'PERCENTWT': 'percentwt',
    'LAB': 'lab_id',
    'PROFILE_ID': 'profile_id', # NEED TO INCLUDE.
    'SAMPLE_TYPE': 'samptype_id', # NEED TO INCLUDE. 
    'TAXONNAME': 'taxonname',
    'TAXONREPNAME': 'taxonrepname',
    'TAXONRANK': 'taxonrank',
    'TAXONDB': 'taxondb',
    'TAXONDBID': 'taxondb_id',
    'TAXONDBURL': 'taxondb_url',
    'REF_ID': 'ref_id',
    # 'SMP_ID': 'samplabcode',
    'SMP_ID_PROVIDER': 'samplabcode',
    'STATION': 'station'

    # CSV_VARS not included in current workflow.
    # 'SAMPLE_LAB_CODE': 'samplelabcode',
    # 'SED_REPNAME': 'sedrepname',
    # 'MEASURE_NOTE': 'measurenote',
    # 'REFERENCE_NOTE': 'referencenote',
    # 'SAMPLE_NOTE': 'samplenote',
}

# %% ../nbs/api/configs.ipynb #9a7deced
NC_GROUPS = {'BIOTA': 'biota',
             'SEAWATER': 'seawater',
             'SEDIMENT': 'sediment',
             'SUSPENDED_MATTER': 'suspended_matter'}

# %% ../nbs/api/configs.ipynb #6a751b6e
SMP_TYPE_LUT = {
    'SEAWATER': 1,
    'BIOTA': 2,
    'SEDIMENT': 3,
    'SUSPENDED_MATTER': 4
}

# %% ../nbs/api/configs.ipynb #6423b1ee
NC_DTYPES = {
    'AREA': {
        'name': 'area_t', 
        'fname': 'dbo_area.xlsx',
        'key': 'displayName', 
        'value':'areaId'
    },
    'BIO_GROUP': {
        'name': 'bio_group_t', 
        'fname': 'dbo_biogroup.xlsx',
        'key': 'biogroup', 
        'value':'biogroup_id'
    },
    'BODY_PART': {
        'name': 'body_part_t', 
        'fname': 'dbo_bodypar.xlsx',
        'key': 'bodypar', 
        'value':'bodypar_id'
    },
    'COUNT_MET': {
        'name': 'count_met_t', 
        'fname': 'dbo_counmet.xlsx',
        'key': 'counmet',
        'value':'counmet_id'
    },
    'DL': {
        'name': 'dl_t', 
        'fname': 'dbo_detectlimit.xlsx',
        'key': 'name_sanitized', 
        'value':'id'
    },
    'FILT': {
        'name': 'filt_t', 
        'fname': 'dbo_filtered.xlsx',
        'key': 'name',
        'value':'id'
    },
    'NUCLIDE': {
        'name': 'nuclide_t', 
        'fname': 'dbo_nuclide.xlsx',
        'key': 'nc_name',
        'value':'nuclide_id'
    },
    'PREP_MET': {
        'name': 'prep_met_t', 
        'fname': 'dbo_prepmet.xlsx', 
        'key': 'prepmet',
        'value':'prepmet_id'
    },
    'SAMP_MET': {
        'name': 'samp_met_t', 
        'fname': 'dbo_sampmet.xlsx', 
        'key': 'sampmet',
        'value':'sampmet_id'
    },
    'SED_TYPE': {
        'name': 'sed_type_t', 
        'fname': 'dbo_sedtype.xlsx', 
        'key': 'sedtype', 
        'value':'sedtype_id'
    },
    'SPECIES': {
        'name': 'species_t', 
        # 'fname': 'dbo_species_cleaned.xlsx',
        'fname': 'dbo_species_2024_11_19.xlsx',
        'key': 'species', 
        'value':'species_id'
    },
    'UNIT': {
        'name': 'unit_t', 
        'fname': 'dbo_unit.xlsx', 
        'key': 'unit_sanitized', 
        'value':'unit_id'
    },
    'LAB': {
        'name': 'lab_t', 
        #'fname': 'dbo_lab.xlsx', 
        'fname': 'dbo_lab_cleaned.xlsx', 
        'key': 'lab', 
        'value':'lab_id'
    }
}

# %% ../nbs/api/configs.ipynb #f0f94538
CSV_DTYPES = {
    'AREA': {'state': 'decoded'},
    'NUCLIDE': {'state': 'encoded'},  # encoded nuclide_id
    'UNIT': {'state': 'encoded'},  # encoded unit_id
    'DL': {'state': 'decoded'},
    'FILT': {'state': 'decoded'},
    'COUNT_MET': {'state': 'encoded'},  # encoded counmet_id
    'SAMP_MET': {'state': 'encoded'},  # encoded sampmet_id
    'PREP_MET': {'state': 'encoded'},  # encoded prepmet_id
    'SPECIES': {'state': 'encoded'},  # encoded species_id
    'BODY_PART': {'state': 'encoded'},  # encoded bodypar_id
    'SED_TYPE': {'state': 'encoded'},  # encoded sedtype_id
    'LAB': {'state': 'encoded'},  # encoded lab_id
}

# %% ../nbs/api/configs.ipynb #e5b80558
ZOTERO_LIB_ID = '2432820'

# %% ../nbs/api/configs.ipynb #09a81790
def lut_path() -> Path:              # Path to LUTs directory
    "Return the path to the lookup tables directory."
    return _pkg_files('marisco') / 'files/lut'

# %% ../nbs/api/configs.ipynb #8836df19
def lut_fname(key: str               # NC_DTYPES key, e.g. 'SPECIES', 'UNIT', 'DL'
              ) -> Path:             # Full path to the lookup table Excel file
    "Return the full path to a lookup table file by its NC_DTYPES key."
    return lut_path() / NC_DTYPES[key]['fname']

# %% ../nbs/api/configs.ipynb #e6be254c
def nc_tpl_path() -> Path:           # Path to MARIS NetCDF template
    "Return the path to the MARIS NetCDF template file."
    return _pkg_files('marisco') / 'files/nc/maris-template.nc'

# %% ../nbs/api/configs.ipynb #4dca0ad3
def cache_path() -> Path:            # Path to cache directory
    "Return the path to the cache directory (e.g. WoRMS lookups)."
    return Path.home() / '.cache' / 'marisco'

# %% ../nbs/api/configs.ipynb #f63b3971
NETCDF_TO_PYTHON_TYPE = {
    'u8': int,
    'f4': float
    }

# %% ../nbs/api/configs.ipynb #97b6b0ec
def get_time_units() -> str:
    "Get the units attribute of the time variable from a NetCDF file."
    with Dataset(nc_tpl_path(), 'r') as nc:
        for group in nc.groups.values():
            if 'time' in group.variables:
                return group.variables['time'].units
    raise ValueError("Time variable not found in NetCDF file")

# %% ../nbs/api/configs.ipynb #7e994e93
def sanitize(
    s: str|float # String or float to sanitize
    ) -> str|float:  # Sanitized string or original float
    """
    Sanitize dictionary key to comply with NetCDF enumeration type:
    
    - Remove `(`, `)`, `.`, `/`, `-`
    - Strip the string
    - Return original value if it's not a string (e.g., NaN)
    """
    if isinstance(s, str):
        s = re.sub(r'[().]', '', s)
        return re.sub(r'[/-]', ' ', s).strip()
    elif pd.isna(s):  # This covers np.nan, None, and pandas NaT
        return s
    else:
        return str(s).strip()

# %% ../nbs/api/configs.ipynb #57772123
def try_int(x):
    "Try to convert `x` to an integer."
    try:
        return int(x)
    except (ValueError, TypeError):
        return x

# %% ../nbs/api/configs.ipynb #0d172382
def get_lut(
    src_dir: str, # Directory containing lookup tables
    fname: str, # Excel file lookup table name
    key: str, # Excel file column name to be used as dict keys 
    value: str, # Excel file column name to be used as dict values 
    do_sanitize: bool=True, # Sanitization required?
    reverse: bool=False, # Reverse lookup table (value, key)
    check_duplicates: bool=False # Check for duplicates in lookup table
    ) -> Dict[str, int]: # MARIS lookup table (key, value)
    "Convert MARIS db lookup table excel file to dictionary `{'name': id, ...}` or `{id: name, ...}` if `reverse` is True."
    fname = Path(src_dir) / fname
    df = pd.read_excel(fname, usecols=[key, value]).dropna(subset=value)
    
    if check_duplicates:
        duplicates = df[key][df[key].duplicated()].tolist()
        if duplicates: print(f"Warning: {fname.name}: found duplicate keys: {duplicates}")
        
    df[value] = df[value].astype('int')
    df = df.set_index(key)
    lut = df[value].to_dict()
    if do_sanitize: lut = {sanitize(k): v for k, v in lut.items()}
    lut = {try_int(k): try_int(v) for k, v in lut.items()}    
    return {v: k for k, v in lut.items()} if reverse else lut

# %% ../nbs/api/configs.ipynb #5c4b1056
class Enums():
    "Return dictionaries of MARIS NetCDF's enumeration types."
    def __init__(self, 
                 lut_src_dir: str, # Directory containing lookup tables
                 dtypes: Dict[str, Dict[str, str]]=NC_DTYPES, # Custom NetCDF types
                 ):
        fc.store_attr()
        self.types = self.lookup()
        
    def filter(self, var_name, values):
        return {name: id for name, id in self.types[var_name].items() if id in values}
    
    def lookup(self):
        types = {}
        for var_name, dtype in self.dtypes.items():
            name, fname, key, value = dtype.values()
            lut = get_lut(self.lut_src_dir, fname, key=key, value=value)
            types[var_name] = lut
        return types
